server {
    listen 80;
    server_name _;
    absolute_redirect off;

    location = / {
        return 302 /admin/;
    }

    location /admin/ {
        alias /usr/share/nginx/html/admin/;
        try_files $uri $uri/ /admin/index.html;
    }
}
